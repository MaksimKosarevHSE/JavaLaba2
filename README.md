# 🌳Trie

## Описание реализации

- Класс Trie использует вспомогательные классы из самописного пакета utils (LinkedList, HashMap, Pair)
- Бор/префиксное дерево работает с любыми символами кодировки UTF-16 (2^16 символов)
- Для хранения детей каждой вершины используется HashMap
- Дерево сериализируется и сохраняется в файл
  
## Запуск

Запуск программы происходит из класса `com.maksim.TrieProject.TrieProject`, если файл `trie.bin` отсутствует в корне проекта, то он будет создан
  
## Асимптотическая сложность
  Временная сложность вставки и проверки наличия слова - **O(m)**, где m - длинна строки

  Пространственная сложность вставки слова - **O(m)**, где m - сумма длин всех слов
